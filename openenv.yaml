name: customer-support-env
version: "1.0.0"
description: >
  A real-world RL environment where an AI agent handles customer support tickets.
  The agent must classify tickets, respond to customers, and make escalation decisions
  across three tasks of increasing difficulty.

author: hackathon-participant
tags:
  - customer-support
  - nlp
  - real-world
  - triage
  - escalation

tasks:
  - id: easy
    name: easy
    description: Classify the incoming ticket into the correct category.
    difficulty: easy
    grader: graders:grade_easy

  - id: medium
    name: medium
    description: Classify the ticket and write an appropriate customer response.
    difficulty: medium
    grader: graders:grade_medium

  - id: hard
    name: hard
    description: Classify, respond, and make the correct escalation/resolution decision.
    difficulty: hard
    grader: graders:grade_hard

action_space:
  type: discrete
  actions:
    - classify
    - respond
    - escalate
    - resolve

observation_space:
  ticket_id: string
  ticket_text: string
  customer_name: string
  customer_history: object
  current_step: integer
  max_steps: integer
  actions_taken: list
  feedback: string
  task_name: string
  task_description: string
  done: boolean

endpoints:
  health: GET /health
  reset: POST /reset
  step: POST /step
  state: GET /state
  tasks: GET /tasks